import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#83946A",
    },
    secondary: {
      main: "#c5da9cff",
    },
  },
});
export default theme;

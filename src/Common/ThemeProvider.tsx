import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#B6BD53",
    },
    secondary: {
      main: "#c5da9cff",
    },
  },
});
export default theme;

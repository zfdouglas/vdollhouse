import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8db739ff",
    },
    secondary: {
      main: "#c5da9cff",
    },
  },
});
export default theme;
